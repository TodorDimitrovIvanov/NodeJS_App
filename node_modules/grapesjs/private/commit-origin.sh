#!/bin/bash
##echo "The date today is $(date)"
##echo "You are here $PWD"
##myCharVar="Ciao"
commitMsg=""

if [ "$1" ]
then
    commitMsg=$1 
else
    read -p "Hi $(whoami)! Please insert your commit message: " commitMsg
fi
cd /Users/Shared/eclipse2/grapes
sudo git add .
sudo git commit -m "$commitMsg"
sudo git push origin master

echo "Your commit with message '$commitMsg' is done."

function help () {
    echo "Help message"
}
