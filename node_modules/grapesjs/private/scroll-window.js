var timeOut;
scrollInterval = () => {
  console.log('animating scroll222');
  if ( frameWindow.scrollY != nextTop ) {
    var step = nextTop > actualTop ? 1 : -1;
    frameWindow.scrollBy(0, step);
    timeOut = setTimeout(scrollInterval, 10);
  } else {
    clearTimeout(timeOut);
  }
}
